version: '3'

services:
  node-app:
    build: .
    image: node-app
    env_file:
      - .env
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/src/node-app
    networks:
      - node-network

networks:
  node-network:
    driver: bridge
